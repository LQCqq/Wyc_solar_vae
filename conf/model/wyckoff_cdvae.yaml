_target_: cdvae.pl_modules.model.WyckoffCDVAE

#encoder
#encoder:
#  _target_: cdvae.pl_modules.gnn.DimeNetPlusPlusWrap
#  num_targets: 512
#  hidden_channels: 128
#  num_blocks: 4
#  int_emb_size: 64
#  basis_emb_size: 8
#  out_emb_channels: 256
#  num_spherical: 7
#  num_radial: 6
#  otf_graph: ${data.otf_graph}
#  cutoff: 7.0 
#  max_num_neighbors: 20
#  envelope_exponent: 5
#  num_before_skip: 1
#  num_after_skip: 2
#  num_output_layers: 3

# 模型结构
latent_dim: 512
hidden_dim: 512
max_wyckoff_sites: 12   # 每个晶体最多预测的wyckoff位点数
fc_num_layers: 3


# 损失权重
beta: 0.01           # KL 权重，对应原版 hparams.beta
cost_property: 1.0   # 属性损失权重

# Wyckoff 各子损失权重
w_spg: 1.0
w_lattice: 1.0
w_elem: 1.0
w_letter: 1.0
w_free: 1.0
w_overlap: 0.0
w_charge: 0.0

# 数据
use_wyckoff: true
predict_property: false

# 保留原始 CDVAE 中 BaseModule 需要的字段
# （从 vae.yaml 继承，避免 hparams 缺失）
max_atoms: ${data.max_atoms}
teacher_forcing_max_epoch: ${data.teacher_forcing_max_epoch}